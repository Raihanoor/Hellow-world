#include <bits/stdc++.h>
using namespace std;

int main() {
    long long int a,b,diff;
    while(scanf ("%lld %lld", &a, &b)) {
        diff = abs(a - b);
        printf("%lld\n", diff);
    }

    return 0;
}
