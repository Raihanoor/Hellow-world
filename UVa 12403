#include <bits/stdc++.h>

using namespace std;

int main() {
    int t,ia,ta=0;
    string s;
    cin >> t;
    while(t--){
        cin >> s;
        if(s=="donate")
        {
            cin >> ia;
            ta += ia;

        }
        else cout << ta << endl;
    }
    return 0;
}
